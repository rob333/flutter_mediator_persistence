name: flutter_mediator_persistence
description: A super easy state management package with build in persistence capability, base on the Flutter Mediator Lite.
version: 1.0.2+1
homepage: https://github.com/rob333/flutter_mediator_persistence

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
